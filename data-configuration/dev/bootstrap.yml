#-----------------------------------------------------------------------------------------------------------------------
#Bacis' configuration of microservice:
spring:
  application:
    name: ${APPLICATION_NAME}
  cloud:
    config:
      enabled: 'true'
      uri: http://localhost:8888
#-----------------------------------------------------------------------------------------------------------------------
management:
  endpoints:
    web:
      exposure:
        include: '*'
#-----------------------------------------------------------------------------------------------------------------------
